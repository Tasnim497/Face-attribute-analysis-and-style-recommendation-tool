# Face-attribute-analysis-and-style-recommendation-tool
# The face dataset is named as "finaset" and the eye dataset is named as "mergedd_classification_dataset" where both are labelled images retrieved from kaggle and roboflow
